<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Edit Player - <%= player.name %></title>
  <link href="https://fonts.googleapis.com/css2?family=Inter&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body class="bg-gray-100 font-sans text-gray-900">
  <nav class="bg-white shadow p-4 flex justify-between items-center">
    <h1 class="text-2xl font-bold">Edit Player</h1>
    <a href="/teams/<%= player.team_id %>" class="text-blue-600 hover:underline">Back to Team Details</a>
  </nav>
  <main class="max-w-[600px] mx-auto p-6 bg-white rounded shadow mt-6">
    <form action="/teams/<%= player.team_id %>/players/<%= player.id %>/update" method="post" class="space-y-4">
      <div>
        <label for="name" class="block font-semibold mb-1">Name</label>
        <input type="text" id="name" name="name" value="<%= player.name %>" required class="w-full border border-gray-300 rounded px-3 py-2" />
      </div>
      <div>
        <label for="number" class="block font-semibold mb-1">Number</label>
        <input type="number" id="number" name="number" value="<%= player.number %>" required class="w-full border border-gray-300 rounded px-3 py-2" />
      </div>
      <div>
        <label for="position" class="block font-semibold mb-1">Position</label>
        <input type="text" id="position" name="position" value="<%= player.position %>" class="w-full border border-gray-300 rounded px-3 py-2" />
      </div>
      <div>
        <label for="height" class="block font-semibold mb-1">Height</label>
        <input type="text" id="height" name="height" value="<%= player.height %>" class="w-full border border-gray-300 rounded px-3 py-2" />
      </div>
      <div>
        <label for="age" class="block font-semibold mb-1">Age</label>
        <input type="number" id="age" name="age" value="<%= player.age %>" class="w-full border border-gray-300 rounded px-3 py-2" />
      </div>
      <div>
        <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition">
          Update Player
        </button>
      </div>
    </form>
  </main>
</body>
</html>
